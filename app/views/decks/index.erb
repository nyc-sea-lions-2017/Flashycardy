<h1>Flashcard Decks</h1>
<ul>
  <% @decks.each do |deck| %>
	  <% if logged_in %>

	 				<a href="/decks/<%= deck.id %>"><%= deck.deckname %></a>
	  <% else %>
	  	  <%= deck.deckname %></a>
	  <% end %>
	  <br>
  <% end %>
</ul>
